# Database
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/hyperativa?allowPublicKeyRetrieval=true&useSSL=false}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:hyperativa}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:hyperativa123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# application.properties
spring.data.redis.host=${SPRING_REDIS_HOST:redis}
spring.data.redis.port=${SPRING_REDIS_PORT:6379}


# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.jdbc.time_zone=America/Sao_Paulo

# Apache Camel
camel.springboot.main-run-controller=true
camel.springboot.jsp.enabled=false
camel.springboot.stream-cache=true

# Email (kept fixed, but can also be parameterized if desired)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=hyperativaapi@gmail.com
spring.mail.password=sgmi qzjh kysd rbgx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
