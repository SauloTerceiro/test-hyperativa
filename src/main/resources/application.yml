spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hyperativa?allowPublicKeyRetrieval=true&useSSL=false
    username: hyperativa
    password: hyperativa123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect